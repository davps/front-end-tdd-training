import {List} from 'immutable';

export default function reducer(state, action) {
  if (action.type === 'ADD_ROW') {
    List(state.rows).splice(index: number, removeNum: number, ...values: Array<T>): this

  }

  return state;
}
